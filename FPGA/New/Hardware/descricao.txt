S0 - S1 - S2 - S3
Registradores temporários do pipeline
https://teams.microsoft.com/v2/

Malha
Memória que vai representar a malha com dados dos quadrados (disponibilidade, coordenada, ...)
-Tamanho dos dados
logTamanhoMalha | logTamanhoMalha | 2               | 2                | 2logTamanhoMalha | 2logTamanhoMalha | 1 
valor linear X | valor linear Y	 | Valor da Célula | Pai              | Custo Acumulado  | Custo Estimado   | open (se já está na heap ou não
Total: 6 log2(TamanhoMalha) + 5
Ajustar isso

Para Malha 15:
4 | 4 | 2 | 2 | 8 | 8 | 1


FIFO (https://www.chipverify.com/verilog/synchronous-fifo)
Fila para as informações que serão salvas na Heap, como a Heap será em SW, poderá ter atrasos, logo a Fila tentará servirá para o SW consumir os dados e salvar na Heap conforme demanda

0 -> insere na Heap 1 -> heapify | addr
1                                | tamanhoAddr 

vizinhoTop
O topo do projeto onde serão instanciados os elementos e a comunicação com o NIOS será realizada

controler
Controlador dos sinais

decisorMalha
Decide quem pode usar a malha


incrementar no futuro para reset da malha: 
localparam BITS = $clog2(TamanhoMalha);

always_ff @(posedge clk or posedge reset) begin
    if (reset) begin
        int x, y;
        int addr = 0;

        for (y = 0; y < TamanhoMalha; y++) begin
            for (x = 0; x < TamanhoMalha; x++) begin
                
                data[addr] <= { x[BITS-1:0], y[BITS-1:0], {RESTO{1'b0}} };

                addr++;
            end
        end
    end
end
